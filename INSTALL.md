# 安装与恢复

## 一、只安装门店系统

1. 从仓库最新 Release 下载 `miaoda-yixinyiyi-v*.zip`。
2. 登录飞书妙搭。
3. 选择“导入新建应用”，上传 ZIP。
4. 等待代码、依赖和数据库迁移初始化完成。
5. 在预览中检查页面和老板身份。
6. 确认无误后发布，并把正式运行链接交给门店员工。

## 二、恢复本门店历史数据

1. 确认目标是新建应用，所有业务表为空。
2. 从私有 Release 下载加密备份 `*.store-backup.enc`。
3. 运行：

   ```bash
   ./scripts/decrypt-store-backup.sh backup.store-backup.enc backup.store-backup.dec.json
   ```

4. 用户本人输入解密密码。
5. 使用老板账号进入 `员工与权限 → 备份与恢复`。
6. 点击“选择备份恢复”，选择解密后的 JSON；文件结构和14张数据表校验通过后，输入`确认恢复门店数据`。
7. 点击“确认恢复到空数据库”。服务端还会再次校验文件、老板身份和空数据库；任一业务表非空都会拒绝恢复。
8. 恢复成功后立即安全删除本机明文备份。
9. 按 `docs/RESTORE_CHECKLIST.md` 验收。

## 三、客户最终怎么使用

GitHub 和妙搭编辑器只给老板/实施人员。员工只使用发布后的 `feishuapp.com` 运行链接。
